#include <stdio.h>
#include <stdlib.h>

/* Main function takes a count of arguments and an array of string
 * arguments (represented as a pointer to an array of pointers of chars.
 * The array of poiners to chars is the array of string arguments
 */

int main(int argc, char** argv) {
	printf("InProgram1 ");
	return 0;
}